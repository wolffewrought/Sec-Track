# The one line this build stopped for: the folder-writer library is
# AndroidX, and AndroidX must be switched on by name. Android Studio
# writes this file for you; a hand-built project has to say it itself.
android.useAndroidX=true

# headroom for the build daemon on a small CI runner
org.gradle.jvmargs=-Xmx2g
